## 项目当前任务交接说明（图片上传模块）

### 一、当前在做什么

正在实现「图片上传最小闭环」，目标是：

- 支持图片上传（前端压缩 + 后端存储）
- 返回 `fileId`
- 列表显示方形缩略图
- 点击缩略图可查看完整图片（压缩后图）

这是一个**基础能力模块**，后续要接入三个业务页面：

- 我的仓库（inventory）
- 盈亏统计（trade）
- 公开交易区（public-post）

------

### 二、本轮做到哪里中断了

任务**被强制中断在“基础能力完成，但业务未接入”阶段**

也就是：

> 图片系统本身已经能用，但还没有接入真实业务页面

------

### 三、已经完成的部分（已验证可用）

#### 1️⃣ 后端

已实现：

- 表：`file_asset`
- 上传接口：
  - `POST /api/files/images`
- 预览接口：
  - `GET /api/files/{fileId}/preview`
- 存储：
  - 本地磁盘（./.local-data/uploads）
- 权限：
  - 私有图片：仅本人可访问
  - 公共图片：匿名可访问
- 限制：
  - 支持格式：jpg/png/webp
  - 最大大小：614400 bytes（≈600KB）

#### 2️⃣ 前端

已实现：

- 图片压缩工具：
  - 目标 ≤ 400KB
  - 可放宽到 ≤ 500KB
- 上传 API 封装：
  - `uploadImage`
- 预览 API：
  - blob 拉取
- 组件：
  - `SquareImagePreview`（方形缩略图 + 点击预览）
  - `ImageUploadDemoPanel`（演示上传面板）
- 已挂载到：
  - WarehouseView（仅 demo 用）

#### 3️⃣ 已验证

- 登录 ✔
- 上传 ✔
- 返回 fileId ✔
- 私有图鉴权访问 ✔
- 公共图匿名访问 ✔
- 缩略图展示 ✔
- 点击查看全图 ✔

------

### 四、当前断点（非常关键）

当前状态：

> ❗ 图片能力“已存在”，但**完全没有接入业务数据结构**

具体缺失：

- 业务表（inventory / trade / public_post）**没有使用 `imageFileId`**
- 表单中没有图片上传入口
- 列表中没有图片缩略图
- 没有图片替换 / 删除逻辑

------

### 五、下一步必须做的（按顺序执行）

⚠️ 不要扩展新功能，只做接入

#### Step 1（必须先做）

接入「我的仓库」模块：

1. 表单（新增 / 编辑）：
   - 增加图片上传
   - 保存 `imageFileId`
2. 列表页：
   - 显示缩略图（SquareImagePreview）
   - 使用 `fileId` 渲染

------

#### Step 2

完善图片生命周期：

- 替换图片时：
  - 是否覆盖 or 新上传（推荐新上传）
- 删除业务记录时：
  - 暂不删文件（简单策略）

------

#### Step 3

再复制到：

- 盈亏统计（trade）
- 公开交易区（public-post）

------

### 六、明确不要做的事情（避免跑偏）

当前阶段不要做：

- ❌ MinIO 接入
- ❌ 图片裁剪/编辑
- ❌ 多图上传
- ❌ CDN / 防盗链
- ❌ 图片删除接口（可以后面补）

------

### 七、一句话总结当前状态

> 图片上传能力已完成（底层 OK），但业务层完全没接上，现在要做的是把 `fileId` 接入各业务表单和列表。