# 工程基线文档

本文档定义 `aobidao-web` 的上线工程底线。

它只回答一个问题：

“这个前后端项目要按照什么工程方式去搭，后面才能真正上线而不返工？”

## 1. 目标

统一以下内容：

- 会话与鉴权
- 邮件验证码
- 环境变量与密钥管理
- 前后端部署关系
- CORS / HTTPS
- 文件上传与对象存储
- 日志、监控、备份
- 环境分层

## 2. 默认部署形态

当前默认采用单机 / 单 VPS 可上线架构。

推荐结构：

- `Nginx`
- `frontend` 静态资源
- `backend` Spring Boot
- `MySQL 8`
- `Redis`
- `MinIO`

运行方式：

- 开发：本地分别启动
- 测试 / 生产：`Docker Compose`

当前明确不做：

- 微服务
- Kubernetes
- MQ
- 服务注册发现

## 3. 环境分层

至少区分：

- `local`
- `test`
- `prod`

要求：

- `local` 用于开发与联调
- `test` 尽量接近生产
- `prod` 必须独立密钥、独立数据库、独立对象存储

## 4. 前后端访问关系

默认部署建议：

- 前端：`https://your-domain.com`
- API：`https://your-domain.com/api`

即：

- `Nginx` 统一反代
- 尽量同域部署

好处：

- 降低 CORS 复杂度
- 简化 token / cookie 场景
- 简化上线配置

## 5. 会话与鉴权基线

后端使用：

- `Sa-Token`

### 当前默认策略

- 使用 `token` 模式
- 通过请求头携带 token

推荐请求头：

- `Authorization: Bearer <token>`

### 当前默认会话规则

- 登录成功后签发 token
- token 有明确过期时间
- 主动退出时服务端注销
- 可启用滑动续期，但需控制上限

推荐默认值：

- token 有效期：`7 天`
- 活跃续期：开启
- 最大连续续期上限：`30 天`

### 登录保护

必须实现：

- 登录失败计数
- 邮箱维度限流
- IP 维度限流
- 短时封禁

推荐默认值：

- 单邮箱 10 分钟内失败超过 `5` 次，临时限制登录

## 6. 注册与邮箱验证码基线

注册流程固定为：

1. 输入邮箱
2. 发送验证码
3. 输入验证码和密码
4. 完成注册

### 验证码规则

必须：

- 6 位数字
- 存入 `Redis`
- 有过期时间
- 使用后立即失效

推荐默认值：

- 过期时间：`5 分钟`
- 同邮箱发送间隔：`60 秒`
- 同邮箱 1 小时最多发送：`10 次`

### 邮件服务

当前允许：

- SMTP
- 第三方邮件服务 API

必须满足：

- 发信凭证只走环境变量
- 不写死在代码里
- 正式环境使用真实可投递域名

## 7. 密码安全基线

必须遵守：

- 使用 `BCrypt`
- 不保存明文密码
- 不记录明文密码到日志
- 修改密码时校验旧密码

推荐密码规则：

- 长度不少于 `8`
- 必须包含字母和数字

## 8. CORS / HTTPS / 真实 IP

### CORS

如果前后端不同域部署，必须显式配置允许来源。

禁止：

- 生产环境使用 `*`
- 不加限制地开放所有来源

### HTTPS

正式环境必须：

- 全站 HTTPS
- API 仅通过 HTTPS 暴露

禁止：

- 正式环境明文 HTTP 登录
- 明文传输密码和 token

### 真实 IP

经 `Nginx` 反代时，必须处理真实 IP。

要求：

- `Nginx` 透传标准代理头
- 后端谨慎读取可信代理头
- 限流时不能误用代理 IP

## 9. 文件上传与对象存储

对象存储默认使用：

- `MinIO`

### 上传范围

当前仅允许图片上传。

允许类型建议：

- `image/jpeg`
- `image/png`
- `image/webp`

### 大小限制

推荐默认值：

- 单图不超过 `5MB`

后端必须校验：

- 文件类型
- 文件大小
- 是否为空文件

### 存储规则

必须遵守：

- 文件本体不入库
- 业务表只存文件路径与元数据
- 删除业务记录时，要考虑文件引用清理

### 访问策略

当前推荐：

- 默认私有 bucket
- 通过签名 URL 或受控地址访问

## 10. 环境变量基线

以下内容必须通过环境变量注入：

- 数据库地址与账号密码
- Redis 地址与密码
- Sa-Token 密钥配置
- 邮件服务凭证
- MinIO 凭证
- MinIO bucket 名称
- 前端 API Base URL
- 正式域名配置

必须提供：

- `frontend/.env.example`
- `backend/.env.example`
- `deploy/env/*.env.example`

示例文件中不得包含真实密钥。

## 11. 日志、监控、备份

### 日志

后端至少区分：

- 应用运行日志
- 错误异常日志
- 关键业务操作日志

禁止日志输出：

- 明文密码
- 验证码
- 完整 token
- 各类密钥和凭证

前端生产环境要求：

- 清理无意义 `console.log`
- 不输出敏感信息

### 监控

当前阶段不要求重型平台，但至少要能观察：

- 服务是否存活
- 错误是否明显增加
- 邮件发送是否失败
- 文件上传是否失败

### 备份

正式环境必须有：

- MySQL 备份
- 对象存储关键文件备份或冗余策略

推荐最低要求：

- 每日数据库备份
- 至少保留最近 `7` 天

## 12. 数据迁移基线

数据库脚本应区分：

- 初始化脚本
- 版本迁移脚本

建议后续接入：

- `Flyway`

## 13. 本地开发基线

本地开发推荐：

- 前端独立 dev server
- 后端独立运行
- MySQL / Redis / MinIO 使用本地或 Docker

本地必须保证：

- 启动说明明确
- 示例环境变量齐全
- 新机器可较快跑起来

## 14. 首次上线最低检查项

首次上线前至少确认：

1. 登录、注册、修改密码可用
2. 邮箱验证码发送可用
3. MySQL、Redis、MinIO 连接正常
4. HTTPS 生效
5. CORS 配置正确
6. 上传限制已生效
7. 敏感配置未写入仓库
8. 日志无敏感信息泄露
9. 数据库备份方案已准备

## 15. 当前结论

当前工程基线可概括为：

- 单体后端
- 前后端分离
- token 鉴权
- 邮箱验证码注册
- Redis 负责验证码、限流、幂等
- MinIO 负责图片存储
- Nginx 统一反代
- Docker Compose 作为部署起点
