# aobidao-web 开发指南

## 轻量可上线标准（最高执行原则）

本项目后续开发默认按以下标准执行：

- 个人项目，可上线
- 目标用户量 `1000 ~ 2000`
- 目标并发 `200` 以内
- 不使用大厂冗余架构
- 不提前铺未来功能
- 代码保持精简、干净、好维护
- 一次只做一个功能闭环

如果“工程完整性”和“轻量可维护”冲突：

- 优先选择轻量可维护
- 优先选择当前真实需要
- 优先选择更少类、更少层级、更少耦合

## 当前执行总则（后续每次修改前必须先读）

- 做完一个小功能，就commit到本地dev分支！！
- 先做能跑的最小闭环，再补文档和扩展
- 修改和拓展功能的时候优先复用已有后端接口。
- 一次只推进一个很小的功能切片
- 每改完一块立刻编译或验证
- 不再先铺太多未来结构
- 优先让我尽快交付“可用结果”，而不是“完整设计”
- 超出当前 1000~2000 用户、200 并发目标的预铺设计，默认不做
- 能用单体、单库、少量 service 解决的问题，不额外拆复杂层次
- 能复用现有 DTO / VO / service 的场景，不为“看起来规范”再造一层

每次开发前先读本文件，再按需读取对应文档。

---

## 编码保护红线（每次修改前都必须遵守）

- 所有文本文件默认保持 `UTF-8`，禁止擅自改成其他编码。
- 禁止把中文文案改写成 `\uXXXX` 形式的 Unicode 转义。
- 禁止在代码文件里混入全角引号、全角括号、异常空白字符、不可见字符。
- 如果只是局部改动，禁止整文件重写，优先做最小补丁，避免把原文件编码带坏。
- 修改带中文的 `vue / ts / java / yml / md` 文件后，必须立刻做一次编译或校验，不带着乱码继续往下改。
- 一旦出现乱码、引号异常、标签断裂、字符串截断，先停下来修编码问题，再继续做业务逻辑。

## 当前项目完整执行流程（强制执行）

### A. 开工前

1. 先读 `guide.md`
2. 再读 `PROJECT_STATUS.md`，确认真实进度
3. 再按任务类型补读：
   - 后端业务：`AOBIDAO_WEB_SDD.md` + `DB_SCHEMA.md` + `API_DESIGN.md`
   - 页面复刻：`AOBIDAO_WEB_SDD.md` + `PAGE_REPLICATION_CHECKLIST.md` + 原小程序代码
4. 只定义当前这一小步的 Done，不定义整页、整模块、整阶段

### B. 当前阶段总路线

1. 先收口当前已经开工的后端闭环
2. 再完成图片上传最小闭环
3. 再开始接前端真实页面：
   - 我的仓库
   - 盈亏统计
   - 公开交易区
4. 再补关于页真实内容
5. 最后做联调、回归、部署

### C. 每次开发的固定动作

1. 一次只推进一个最小功能切片
2. 优先改后端真实接口或前端真实页面中的一小块
3. 改完立刻编译或验证
4. 验证通过后，才进入下一小块
5. 如果验证失败，先修到通过，不带着错误继续推进

### D. 当前项目的切片粒度要求

允许的切片示例：

- 修一个编译错误
- 完成一个分页接口
- 完成一个详情接口
- 完成一个新增弹层提交
- 完成一个列表页数据接入

禁止的切片示例：

- “把我的仓库整个做完”
- “把三大页面一起接完”
- “顺手把上传、公开、统计都做了”

### E. 每轮收尾

1. 回看本轮是否真的形成可用结果
2. 更新 `PROJECT_STATUS.md`
3. 如果文档与代码不一致，先修正文档
4. 下一轮继续从 `guide.md` 开始

---

## 进度锚点

当前真实进度以 `PROJECT_STATUS.md` 为准。

---

## 技能索引

| 模块 | 源文档 |
|------|--------|
| 搭项目骨架 | SDD → PROJECT_STRUCTURE → ENGINEERING_BASELINE |
| 登录/注册/修改密码 | SDD → ENGINEERING_BASELINE → DB_SCHEMA → API_DESIGN |
| 数据库设计 | SDD → ENGINEERING_BASELINE + 原小程序代码 → DB_SCHEMA |
| 接口设计 | SDD → DB_SCHEMA → ENGINEERING_BASELINE + 原小程序代码 → API_DESIGN |
| 页面复刻 | SDD → PAGE_REPLICATION_CHECKLIST → 原小程序代码 |
| 三大业务页后端 | SDD → DB_SCHEMA → API_DESIGN → 原小程序代码 |
| 上线部署 | SDD → ENGINEERING_BASELINE → PROJECT_STRUCTURE |
| 排错重构 | SDD → 最相关专项文档 |

---

## 最小可用功能迭代原则（每次必遵守）

- 每次只实现当前任务明确要求的最小功能点，不提前扩展、不顺便优化、不脑补附加需求
- 不跨模块实现、不提前写未来才需要的代码
- 功能未明确、边界未澄清时，禁止直接编写完整实现
- 禁止一次性写完整个页面、整个模块、整套接口，必须拆小、分步实现
- 每完成一个最小功能点，立即验证可用性，再进入下一个点
- 除非当前需求直接需要，否则不新增“为未来准备”的抽象层、基类、策略类、适配器
- 除非当前代码已明显失控，否则不为了形式统一而做大规模重构

---

## 项目红线（最核心禁止行为）

1. 禁止擅自改技术栈（前端Vue3+TS / 后端SpringBoot3.5.x / Sa-Token / MySQL8 / Redis / MinIO）
2. 禁止在controller堆叠核心业务、禁止保存明文密码
3. 禁止未读原小程序代码就脑补页面布局/字段/交互
4. 禁止文档与代码冲突时以文档为准（以代码真实状态为准并修正文档）
5. 前后端分离，用户只能操作自己的数据

详细红线以 `AOBIDAO_WEB_SDD.md` 为准。

---

## 文档优先级

SDD > PROJECT_STATUS > ENGINEERING_BASELINE > DB_SCHEMA/API_DESIGN > PROJECT_STRUCTURE > PAGE_REPLICATION_CHECKLIST
