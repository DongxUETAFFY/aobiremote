# aobidao-web 项目核心 SDD

本文件是 `aobidao-web` 的主规范，只保留三类内容：

- 不可违反的红线
- 当前阶段的开发顺序
- 文档之间的优先级

接口、表结构、页面细节、部署参数不在本文件展开。

## 1. 项目目标

将原微信小程序尽量 1:1 迁移为可上线的 Web 系统，并彻底脱离：

- 微信登录
- 微信云开发 / 云函数 / 云存储
- 小程序运行时和 API

Web 版必须尽量保留原项目的：

- 页面结构
- 视觉风格
- 交互逻辑
- 业务规则

## 2. 文档优先级

文档冲突时，按以下优先级处理：

1. `AOBIDAO_WEB_SDD.md`
2. `ENGINEERING_BASELINE.md`
3. `DB_SCHEMA.md` / `API_DESIGN.md`
4. `PROJECT_STRUCTURE.md`
5. `PAGE_REPLICATION_CHECKLIST.md`
6. README 与说明性文档

文档职责：

- `AOBIDAO_WEB_SDD.md`：红线、阶段顺序、开发底线
- `MASTER_SKILL_INDEX.md`：任务到文档的索引入口，指导按需阅读
- `ENGINEERING_BASELINE.md`：上线工程基线
- `PROJECT_STRUCTURE.md`：目录与模块结构
- `PAGE_REPLICATION_CHECKLIST.md`：页面与交互复刻要点
- `DB_SCHEMA.md`：数据结构
- `API_DESIGN.md`：接口契约

## 3. 当前阶段优先级

当前仍处于“搭骨架、定规范、定结构”阶段。

默认顺序：

1. 主规范与工程基线
2. 项目结构
3. 认证与账号体系基线
4. 数据库设计
5. 接口设计
6. 前后端脚手架
7. 三个核心业务页
8. 体验优化

当前阶段禁止：

- 未定认证策略就先写业务接口
- 未定表结构就大量写接口
- 未定接口就大量写页面细节
- 未读原项目代码就直接做高还原复刻

## 4. 技术选型红线

未经用户明确同意，不得擅自变更：

- 前端：`Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus`
- 后端：`Spring Boot 3.5.x`
- ORM：`MyBatis-Plus`
- 鉴权：`Sa-Token`
- 数据库：`MySQL 8`
- 缓存：`Redis`
- 文件存储：`MinIO`
- 部署：`Docker Compose + Nginx + Spring Boot`

禁止改成：

- 微服务
- 微信体系登录
- 腾讯云函数
- JSP / Thymeleaf 主导页面直出
- 无 TypeScript 的前端方案

## 5. 开工前强制动作

每次开始新任务前，必须先明确：

1. 本轮目标：
   - 文档设计
   - 前端搭建
   - 后端搭建
   - 联调
   - 修复问题
2. 本轮影响范围：
   - 前端
   - 后端
   - 接口契约
   - 数据库结构
   - 页面复刻
3. 需要先读的资料：
   - 原小程序代码
   - 当前 SDD
   - 目录规划文档
   - 已有接口 / 数据库 / 页面文档

目标不清楚时，先澄清，不直接写代码。

## 6. 原项目参考源红线

原微信小程序是最高参考源。

参考顺序：

1. 原项目代码
2. 原项目实际效果
3. README / README2.0
4. 用户补充说明

以下内容禁止脑补，必须先读原代码：

- 页面布局
- 卡片结构
- 弹层结构
- 表单字段
- 按钮文案
- 页面联动
- 排序 / 筛选
- 公开 / 取消公开逻辑
- 统计口径

若原代码与 README 冲突：

- 优先相信当前真实代码
- 标记差异
- 必要时向用户说明

## 7. 架构与命名红线

必须遵守：

- 前后端分离
- 后端单体架构
- 核心业务逻辑全部在 `service` 层
- 前端不承担最终业务一致性

禁止：

- 在 controller 堆叠核心业务
- 在 mapper 写业务逻辑
- 把项目退化为模板直出站点

命名要求：

- Java 包名不得使用 `com.example`
- 前后端业务域命名保持统一语义
- 路由、目录、模块、接口命名不得漂移

## 8. 认证与安全红线

账号体系固定为：

- 注册：邮箱 + 邮箱验证码 + 密码
- 登录：邮箱 + 密码
- 修改密码：旧密码 + 新密码

必须遵守：

- 密码使用 `BCrypt`
- 禁止保存明文密码
- 验证码进入 `Redis`
- 验证码有过期时间
- 验证码发送必须限频
- 登录失败必须限流
- 所有写操作做参数校验
- 用户只能操作自己的数据
- 不能信任前端传来的权限状态

在开始大规模写认证代码前，必须先在 `ENGINEERING_BASELINE.md` 中定下：

- `Sa-Token` 使用模式
- 登录态有效期 / 续期 / 退出策略
- 邮件发送方案
- CORS / HTTPS 策略
- 真实 IP 获取方式
- 上传白名单与大小限制
- 环境变量与密钥管理方式

## 9. 前端开发规则

前端目录按以下语义组织：

- `views`
- `components`
- `stores`
- `api`
- `types`
- `utils`
- `styles`

前端开发顺序：

1. 路由
2. 页面骨架
3. 主要布局
4. 状态管理
5. 接口契约对齐
6. 接口接入
7. 细节样式
8. 体验优化

必须明确页面状态：

- 初始态
- 加载中
- 空态
- 错误态
- 无权限态（如适用）
- 提交中 / 禁用态（如适用）

禁止：

- 所有状态堆进一个 store
- 直接套后台模板替代原项目风格

## 10. 后端开发规则

后端开发顺序：

1. 表结构
2. 枚举和值域
3. DTO / VO
4. 接口设计
5. service
6. controller
7. 缓存 / 上传 / 限流

后端必须坚持：

- controller 只做收参与返回
- service 负责业务规则
- mapper 只做数据访问
- convert 负责对象转换

接口必须统一：

- 响应结构
- 异常处理
- 参数校验
- 登录态校验

前后端并行开发时，先定接口，再接页面。

## 11. 业务一致性红线

以下业务规则必须保留：

1. 仓库卖出后进入盈亏统计
2. 私有记录公开后同步进入公开交易区
3. 取消公开后，公开区同步失效或消失
4. 在公开区删除自己的公开记录后，私有页状态要回写
5. 不可信标记必须防重复
6. 公开 / 取消公开必须限频
7. 日期不得超过当天
8. 用户只能操作自己的私有数据

凡是改动：

- 我的仓库
- 盈亏统计
- 公开交易区

任一页逻辑，都必须同步考虑另外两页。

## 12. 数据与存储红线

必须遵守：

- 优先使用 `MySQL 8`
- 数据库设计围绕业务，不做过度抽象
- `Redis` 只用于验证码、限流、幂等、公开节流等明确场景
- 图片统一走对象存储
- 文件本体不入库
- 业务表只存文件引用与元数据

## 13. 页面复刻规则

默认要复刻：

- 登录页
- 注册页
- 修改密码页
- 我的仓库
- 盈亏统计
- 公开交易区
- 关于页

高还原核心页：

- 我的仓库
- 盈亏统计
- 公开交易区
- 关于页

具体页面结构与交互，以：

- `PAGE_REPLICATION_CHECKLIST.md`

为准。

## 14. 调试与收尾规则

排错顺序必须是：

1. 先复现
2. 先定位前端 / 后端 / 接口 / 数据 / 环境
3. 查日志与现有代码路径
4. 小范围修复
5. 再回归影响面

禁止：

- 没定位清楚就大面积改代码
- 用额外重复请求掩盖问题

每轮开发后至少检查：

- 本轮目标是否完成
- 是否破坏结构
- 是否引入额外耦合
- 是否遗漏文档同步
- 是否留下半成品占位代码
- 是否留下与当前方案冲突的旧实现

## 15. 一句话总纲

AI 在 `aobidao-web` 中必须遵循：

“以原小程序为最高参考源，先定主规范和工程基线，再定结构、接口、数据与页面；优先保证可上线和业务一致性，在既定技术栈内做高还原 Web 重构，不擅自引入微信依赖，不擅自改技术路线，不擅自改变核心交互。”
